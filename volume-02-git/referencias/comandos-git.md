# Comandos usados no Volume 02

```bash
git --version
git config --global --list
git init
git status
git add
git commit
git log --oneline --graph --decorate --all
git diff
git diff --staged
git restore
git restore --staged
git revert
git branch
git switch
git merge
git remote -v
git clone
git fetch
git pull
git push
git tag
git stash
git rebase
```

Consulte o livro antes de usar comandos destrutivos. Em especial, não trate
`reset --hard` ou force push como comandos de rotina.
