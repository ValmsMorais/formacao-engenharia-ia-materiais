# Processador de Transações

## O problema
Explique em poucas linhas o que o projeto resolve.

## Pré-requisitos
- Python 3.x
- Git

## Instalação
```bash
python -m venv .venv
python -m pip install -r requirements.txt
```

## Execução
```bash
python -m src.main
```

## Testes
```bash
pytest -q
```

## Estrutura dos dados
Descreva as colunas do CSV de entrada.

## Limitações atuais
Liste limitações conhecidas.

## Roadmap
- [ ] Persistência em PostgreSQL (Volume 03)
- [ ] API HTTP com FastAPI (Volume 04)
