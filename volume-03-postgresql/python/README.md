# Exemplos Python

Instale as dependências da raiz:

```bash
python -m pip install -r requirements.txt
```

Para o exemplo Psycopg, defina uma DSN PostgreSQL local, por exemplo:

```text
PSYCOPG_DSN=postgresql://postgres:SUA_SENHA@localhost:5432/engenharia_ia
```

Não versione senha real.
